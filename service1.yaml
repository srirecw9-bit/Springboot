apiVersion: v1

kind: Service

metadata:
  name: springboot-service
  namespace: springboot

spec:

  type: NodePort

  selector:
    app: springboot-k8s-demo

  ports:

    - protocol: TCP
      port: 8080
      targetPort: 8080